CosNotification(3)         Erlang Module Definition         CosNotification(3)



NNAAMMEE
       CosNotification - This module export functions which return QoS and
       Admin Properties constants.

DDEESSCCRRIIPPTTIIOONN
       To get access to all definitions include necessary _h_r_l files by using:
       _-_i_n_c_l_u_d_e___l_i_b_(_"_c_o_s_N_o_t_i_f_i_c_a_t_i_o_n_/_i_n_c_l_u_d_e_/_*_._h_r_l_"_)_.

EEXXPPOORRTTSS
       ''EEvveennttRReelliiaabbiilliittyy''(()) -->> ssttrriinngg(())

              This function returns the EventReliability QoS identifier

       ''BBeessttEEffffoorrtt''(()) -->> sshhoorrtt(())

              This function returns the BestEffort QoS value.

       ''PPeerrssiisstteenntt''(()) -->> sshhoorrtt(())

              This function returns the Persistent QoS value.

       ''CCoonnnneeccttiioonnRReelliiaabbiilliittyy''(()) -->> ssttrriinngg(())

              This function returns the ConnectionReliability QoS identifier.

       ''PPrriioorriittyy''(()) -->> ssttrriinngg(())

              This function returns the Priority QoS identifier.

       ''LLoowweessttPPrriioorriittyy''(()) -->> sshhoorrtt(())

              This function returns the LowestPriority QoS value.

       ''HHiigghheessttPPrriioorriittyy''(()) -->> sshhoorrtt(())

              This function returns the HighestPriority QoS value.

       ''DDeeffaauullttPPrriioorriittyy''(()) -->> sshhoorrtt(())

              This function returns the DefaultPriority QoS value.

       ''SSttaarrttTTiimmee''(()) -->> ssttrriinngg(())

              This function returns the StartTime QoS identifier.

       ''SSttooppTTiimmee''(()) -->> ssttrriinngg(())

              This function returns the StopTime QoS identifier.

       ''TTiimmeeoouutt''(()) -->> ssttrriinngg(())

              This function returns the Timeout QoS identifier.

       ''OOrrddeerrPPoolliiccyy''(()) -->> ssttrriinngg(())

              This function returns the OrderPolicy QoS identifier.

       ''AAnnyyOOrrddeerr''(()) -->> sshhoorrtt(())

              This function returns the AnyOrder QoS value.

       ''FFiiffooOOrrddeerr''(()) -->> sshhoorrtt(())

              This function returns the FifoOrder QoS value.

       ''PPrriioorriittyyOOrrddeerr''(()) -->> sshhoorrtt(())

              This function returns the PriorityOrder QoS value.

       ''DDeeaaddlliinneeOOrrddeerr''(()) -->> sshhoorrtt(())

              This function returns the DeadlineOrder QoS value.

       ''DDiissccaarrddPPoolliiccyy''(()) -->> ssttrriinngg(())

              This function returns the DiscardPolicy QoS identifier.

       ''LLiiffooOOrrddeerr''(()) -->> sshhoorrtt(())

              This function returns the LifoOrder QoS value.

       ''RReejjeeccttNNeewwEEvveennttss''(()) -->> sshhoorrtt(())

              This function returns the RejectNewEvents QoS value.

       ''MMaaxxiimmuummBBaattcchhSSiizzee''(()) -->> ssttrriinngg(())

              This function returns the MaximumBatchSize QoS identifier.

       ''PPaacciinnggIInntteerrvvaall''(()) -->> ssttrriinngg(())

              This function returns the PacingInterval QoS identifier.

       ''SSttaarrttTTiimmeeSSuuppppoorrtteedd''(()) -->> ssttrriinngg(())

              This function returns the StartTimeSupported QoS identifier.

       ''SSttooppTTiimmeeSSuuppppoorrtteedd''(()) -->> ssttrriinngg(())

              This function returns the StopTimeSupported QoS identifier.

       ''MMaaxxEEvveennttssPPeerrCCoonnssuummeerr''(()) -->> ssttrriinngg(())

              This function returns the MaxEventsPerConsumer QoS identifier.

       ''MMaaxxQQuueeuueeLLeennggtthh''(()) -->> ssttrriinngg(())

              This function returns the MaxQueueLength Admin identifier.

       ''MMaaxxCCoonnssuummeerrss''(()) -->> ssttrriinngg(())

              This function returns the MaxConsumers Admin identifier.

       ''MMaaxxSSuupppplliieerrss''(()) -->> ssttrriinngg(())

              This function returns the MaxSuppliers Admin identifier.



Ericsson AB                 cosNotification 1.1.18          CosNotification(3)
