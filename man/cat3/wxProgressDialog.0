wxProgressDialog(3)        Erlang Module Definition        wxProgressDialog(3)



NNAAMMEE
       wxProgressDialog - See external documentation: wxProgressDialog.

DDEESSCCRRIIPPTTIIOONN
       See external documentation: wxProgressDialog.

       This class is derived (and can use functions) from:
       wwxxDDiiaalloogg
       wwxxTTooppLLeevveellWWiinnddooww
       wwxxWWiinnddooww
       wwxxEEvvttHHaannddlleerr

DDAATTAA TTYYPPEESS
         _w_x_P_r_o_g_r_e_s_s_D_i_a_l_o_g_(_):


           An object reference, The representation is internal and can be
           changed without notice. It can't be used for comparsion stored on
           disc or distributed for use on other nodes.

EEXXPPOORRTTSS
       nneeww((TTiittllee,, MMeessssaaggee)) -->> wwxxPPrrooggrreessssDDiiaalloogg(())

              Types:

                 Title = chardata() (see module unicode)
                 Message = chardata() (see module unicode)

              Equivalent to nneeww((TTiittllee,, MMeessssaaggee,, [[]])).

       nneeww((TTiittllee,, MMeessssaaggee,, OOppttiioonn::::[[OOppttiioonn]])) -->> wwxxPPrrooggrreessssDDiiaalloogg(())

              Types:

                 Title = chardata() (see module unicode)
                 Message = chardata() (see module unicode)
                 Option = {maximum, integer()} | {parent, wxWindow() (see
                 module wxWindow)} | {style, integer()}

              See external documentation.

       rreessuummee((TThhiiss)) -->> ookk

              Types:

                 This = wxProgressDialog()

              See external documentation.

       uuppddaattee((TThhiiss)) -->> ookk

              Types:

                 This = wxProgressDialog()

              See external documentation.

       uuppddaattee((TThhiiss,, VVaalluuee)) -->> bboooolleeaann(())

              Types:

                 This = wxProgressDialog()
                 Value = integer()

              Equivalent to uuppddaattee((TThhiiss,, VVaalluuee,, [[]])).

       uuppddaattee((TThhiiss,, VVaalluuee,, OOppttiioonn::::[[OOppttiioonn]])) -->> bboooolleeaann(())

              Types:

                 This = wxProgressDialog()
                 Value = integer()
                 Option = {newmsg, chardata() (see module unicode)}

              See external documentation.

       ddeessttrrooyy((TThhiiss::::wwxxPPrrooggrreessssDDiiaalloogg(()))) -->> ookk

              Destroys this object, do not use object again

AAUUTTHHOORRSS
       _<_>



                                   wx 0.99.2               wxProgressDialog(3)
